# Project Instructions

## GitHub Authentication

- Use the GitHub token configured in `C:\Users\avrca\Documents\Projects\hosts.yml`.
- Do not print, copy, expose, or commit the token value.
- When GitHub CLI authentication is needed, point GitHub tooling to the configured host credentials instead of asking for a new token.
- Treat `hosts.yml` as a local secret file outside this project.

## Kluster

- Do not run `kluster_code_review_auto` for this project.
- Do not run automatic Kluster checks after file creation, file modification, or code changes.
- Only use Kluster if the user explicitly requests it in a later message.

## Editing

- Keep edits scoped to the requested files and task.
- Do not modify `C:\Users\avrca\Documents\Projects\hosts.yml` unless explicitly requested.
